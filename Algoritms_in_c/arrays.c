// Arrays are fixed sized sequenced collection of data items of same data types
// access a particular element = arrayname[index]
// arrays are randomly accesed taking constant time
// types - there are three types which include 1D array 2D array and Multi dimensional arrays
// Operations on array include Traversal, Insertion, Deletion, Searching, Sorting

/*
//traversing an array
int a[50], size, i;
printf("enter size of array: ");
scanf("%d", &size);
printf("Enter elements of array: ");
for(i = 0, i<size, i++)
    {
        scanf("%d", &a[i]);
    }
printf("Elements in array are: ");
for(i=0; i<size; i++)
    {
        printf("%d", a[i]);
    }
*/

